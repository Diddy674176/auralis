# Optional build-time overrides (Settings UI is primary).
# Never put secret API keys in VITE_* vars — keys belong in the browser localStorage via Settings.
VITE_TTS_PROVIDER=none
VITE_TTS_PROXY_URL=
